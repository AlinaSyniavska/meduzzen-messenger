class OAuth {
    constructor(userId, access_token, refresh_token) {
        // this.id = id;
        this.userId = userId;
        this.access_token = access_token;
        this.refresh_token = refresh_token;
    }
}

module.exports = OAuth;

